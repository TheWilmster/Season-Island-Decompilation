/* 
 * This is where the enumerators were stored
 * in the original project but gamemaker
 * doesnt compile enumerators or macros,
 * so this is just a paper weight, LMAO.
 * Enumerators and macros can be found in scr_decomp.
*/

exit;
